{
  "name": "@ngrx/router",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "rm -rf ./release && tsc && cp ./package.json release/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "peerDependencies": {
    "angular2": "^2.0.0-beta.9",
    "rxjs": "^5.0.0-beta.4"
  },
  "devDependencies": {
    "angular2": "^2.0.0-beta.12",
    "rxjs": "^5.0.0-beta.4",
    "typescript": "^1.8.9"
  }
}
